{
  "name": "@disbot/api",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --env-file=../../.env --watch src/index.ts",
    "start": "bun src/index.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "biome check .",
    "lint:fix": "biome check --write ."
  },
  "dependencies": {
    "@disbot/database": "workspace:*",
    "@disbot/shared": "workspace:*",
    "hono": "^4.12.18"
  },
  "devDependencies": {
    "@disbot/typescript-config": "workspace:*",
    "typescript": "^6.0.3",
    "vitest": "^4.1.5"
  }
}
